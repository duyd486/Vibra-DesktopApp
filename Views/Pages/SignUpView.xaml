<UserControl x:Class="Vibra_DesktopApp.Views.SignUpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Vibra_DesktopApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="680" d:DesignWidth="680">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">

            <StackPanel Margin="0 0 0 0" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Border Background="White" Width="200" Height="60">
                    <Border.OpacityMask>
                        <ImageBrush ImageSource="/Assets/Images/MyLogo.png"/>
                    </Border.OpacityMask>
                </Border>
                <TextBlock Text="Đăng ký tài khoản Vibra" FontWeight="Bold" FontSize="36" Margin="0 0 0 0" Foreground="White"/>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1" Margin="0 -10 0 0">
            <Grid.RowDefinitions>
                <RowDefinition Height="4*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>

                </Grid.RowDefinitions>

                <Grid.Resources>
                    <Style TargetType="PasswordBox">
                        <Setter Property="Background" Value="AliceBlue"/>
                        <Setter Property="Foreground" Value="Black"/>
                        <Setter Property="FontSize" Value="18"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="BorderBrush" Value="Gray"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="PasswordBox">

                                    <Border x:Name="border"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="5">
                                        <ScrollViewer x:Name="PART_ContentHost"
                                                  Margin="{TemplateBinding Padding}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="BorderBrush" Value="Gray"/>
                                            <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                        </Trigger>
                                        <Trigger Property="IsFocused" Value="True">
                                            <Setter TargetName="border" Property="BorderBrush" Value="White"/>
                                            <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>


                    <Style TargetType="TextBox">
                        <Setter Property="Background" Value="AliceBlue"/>
                        <Setter Property="Foreground" Value="Black"/>
                        <Setter Property="FontSize" Value="18"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="BorderBrush" Value="Gray"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TextBox">
                                    <Border x:Name="border"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="5">
                                        <ScrollViewer x:Name="PART_ContentHost"
                                                  Margin="{TemplateBinding Padding}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="BorderBrush" Value="Gray"/>
                                            <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                        </Trigger>
                                        <Trigger Property="IsFocused" Value="True">
                                            <Setter TargetName="border" Property="BorderBrush" Value="White"/>
                                            <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Grid.Resources>

                <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="Địa chỉ Email" Foreground="White" FontSize="16" FontWeight="Bold"  />
                    <TextBox Text="{Binding EmailText}" Width="300" Height="40" FontSize="20" Margin="0 10 0 0" VerticalContentAlignment="Center"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Margin="0 -28 0 0" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="Mật khẩu" Foreground="White" FontSize="16" FontWeight="Bold"  />
                    <PasswordBox PasswordChanged="PasswordBox_PasswordChanged" Width="300" Height="40" FontSize="20" Margin="0 10 0 0" VerticalContentAlignment="Center"/>

                </StackPanel>
                <StackPanel Grid.Row="2" Margin="0 -28 0 0" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="Nhập lại mật khẩu" Foreground="White" FontSize="16" FontWeight="Bold"  />
                    <PasswordBox PasswordChanged="PasswordBox_RePasswordChanged" Width="300" Height="40" FontSize="20" Margin="0 10 0 0" VerticalContentAlignment="Center"/>

                </StackPanel>
                <StackPanel Grid.Row="3" Margin="0 -40 0 0" VerticalAlignment="Center" HorizontalAlignment="Center">

                    <Button Width="300" Height="40" Content="Đăng ký" FontWeight="SemiBold" Cursor="Hand" Background="#BC4D15" Command="{Binding SignUpCommand}">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="20"
                                    BorderThickness="0">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#b36b47"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter Property="Background" Value="#DDDDDD"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Grid>

            <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Orientation="Horizontal" Margin="0 0 0 40">
                <TextBlock Text="Bạn đã có tài khoản?  " Foreground="#aeaeae" FontSize="18"/>
                <Button Command="{Binding OpenLoginCommand}" Content="Đăng nhập" FontSize="18" Background="Transparent" BorderThickness="0" Padding="0" Cursor="Hand" Foreground="White"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
