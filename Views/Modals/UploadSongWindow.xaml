<Window x:Class="Vibra_DesktopApp.Views.Modals.UploadSongWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa="clr-namespace:FontAwesome.Sharp;assembly=FontAwesome.Sharp"
        mc:Ignorable="d"
        Title="Đăng tải bài hát"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        Background="Transparent">

    <Border Width="900"
            Padding="24"
            CornerRadius="12"
            Background="{DynamicResource NavActiveBackgroundBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid>
                <TextBlock Text="Đăng tải bài hát"
                           FontSize="22"
                           FontWeight="Bold"
                           Foreground="{DynamicResource CurrentColor}"/>
                <Button Width="32" Height="32"
                        HorizontalAlignment="Right"
                        Background="Transparent"
                        BorderThickness="0"
                        Command="{Binding CloseWindowCommand}"
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}">
                    <TextBlock Text="✕" FontSize="16" Foreground="White"/>
                </Button>
            </Grid>

            <Grid Grid.Row="1" Margin="0 18 0 0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="330"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Width="330" Height="330"
                        BorderThickness="0"
                        Padding="0"
                        Background="{DynamicResource NavInactiveTextBrush}"
                        Command="{Binding ChooseSongThumbnailCommand}">
                    <Border CornerRadius="12" ClipToBounds="True">
                        <Grid>
                            <Image Source="Assets/Images/default.jpg" Stretch="UniformToFill"/>
                            <Image Source="{Binding SongThumbnailPreviewPath}" Stretch="UniformToFill"/>
                            <Border Background="#66000000" Opacity="0">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Opacity" Value="0"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Opacity" Value="1"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <fa:IconBlock Icon="Edit" Foreground="White" FontSize="28"
                                              HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </Border>
                </Button>

                <StackPanel Grid.Column="2">
                    <Button Command="{Binding ChooseSongFileCommand}"
                                 Background="{DynamicResource WindowBackgroundBrush}"
                                 Foreground="{DynamicResource AppTextBrush}"
                            BorderThickness="0"
                            Padding="14,10"
                            Margin="0 0 0 10"
                            Content="{Binding SelectedSongFileName, TargetNullValue=Chọn file Nhạc}"/>

                    <Grid Margin="0 0 0 10">
                        <TextBox x:Name="SongDescriptionTextBox"
                                 Text="{Binding SongDescription, UpdateSourceTrigger=PropertyChanged}"
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 Height="140"
                                 Background="{DynamicResource WindowBackgroundBrush}"
                                 Foreground="{DynamicResource AppTextBrush}"
                                 BorderThickness="0"
                                 Padding="14,10"
                                 FontSize="14" />

                        <TextBlock Text="Thông tin bài hát"
                                   Margin="14,10,14,10"
                                   Foreground="#9CA3AF"
                                   Opacity="0.7"
                                   IsHitTestVisible="False">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding Text, ElementName=SongDescriptionTextBox}" Value="" />
                                                <Condition Binding="{Binding IsKeyboardFocusWithin, ElementName=SongDescriptionTextBox}" Value="False" />
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="Visibility" Value="Visible" />
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>

                    <Grid Margin="0 0 0 10">
                        <TextBox x:Name="SongPriceTextBox"
                                 Text="{Binding SongPrice, UpdateSourceTrigger=PropertyChanged}"
                                 Background="{DynamicResource WindowBackgroundBrush}"
                                 Foreground="{DynamicResource AppTextBrush}"
                                 BorderThickness="0"
                                 Padding="14,10"
                                 FontSize="14" />

                        <TextBlock Text="Giá tiền"
                                   Margin="14,10,14,10"
                                 Background="{DynamicResource WindowBackgroundBrush}"
                                 Foreground="{DynamicResource AppTextBrush}"
                                   Opacity="0.7"
                                   IsHitTestVisible="False">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding Text, ElementName=SongPriceTextBox}" Value="" />
                                                <Condition Binding="{Binding IsKeyboardFocusWithin, ElementName=SongPriceTextBox}" Value="False" />
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="Visibility" Value="Visible" />
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Category dropdown (Vue-like, Popup) -->
                        <Grid Grid.Column="0">
                            <Button x:Name="CategoryDropdownButton"
                                 Background="{DynamicResource WindowBackgroundBrush}"
                                 Foreground="{DynamicResource AppTextBrush}"
                                    BorderThickness="0"
                                    Padding="14,10"
                                    HorizontalContentAlignment="Left"
                                    Command="{Binding ToggleCategoryDropdownCommand}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0"
                                                Text="{Binding SelectedCategoryName, TargetNullValue=Chọn thể loại}"
                                               TextTrimming="CharacterEllipsis"/>
                                    <fa:IconBlock Grid.Column="1" Icon="ChevronDown" Foreground="#B0B0B0" FontSize="14"/>
                                </Grid>
                            </Button>

                            <Popup IsOpen="{Binding IsCategoryDropdownOpen, Mode=TwoWay}"
                                   PlacementTarget="{Binding ElementName=CategoryDropdownButton}"
                                   Placement="Bottom"
                                   VerticalOffset="6"
                                   StaysOpen="False"
                                   AllowsTransparency="True"
                                   PopupAnimation="Fade">
                                <Border Width="{Binding ActualWidth, ElementName=CategoryDropdownButton}"
                                        Background="{DynamicResource NavActiveTextBrush}"
                                        BorderBrush="#3A3533"
                                        BorderThickness="1"
                                        CornerRadius="10"
                                        Padding="6">
                                    <ScrollViewer MaxHeight="280" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                        <ItemsControl ItemsSource="{Binding AllCategories}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Background="Transparent"
                                                            BorderThickness="0"
                                                            Padding="12,10"
                                                            HorizontalContentAlignment="Left"
                                                            Foreground="{DynamicResource AppTextBrush}"
                                                            Command="{Binding DataContext.SelectCategoryCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                            CommandParameter="{Binding}">
                                                        <Button.Style>
                                                            <Style TargetType="Button">
                                                                <Setter Property="Background" Value="Transparent"/>
                                                                <Style.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter Property="Background" Value="#3A3533"/>
                                                                    </Trigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Button.Style>
                                                        <TextBlock Text="{Binding name}"/>
                                                    </Button>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>

                        <!-- Album dropdown (Vue-like, Popup) -->
                        <Grid Grid.Column="2">
                            <Button x:Name="AlbumDropdownButton"
                                 Background="{DynamicResource WindowBackgroundBrush}"
                                 Foreground="{DynamicResource AppTextBrush}"
                                    BorderThickness="0"
                                    Padding="14,10"
                                    HorizontalContentAlignment="Left"
                                    Command="{Binding ToggleAlbumDropdownCommand}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0"
                                                Text="{Binding SelectedAlbumName, TargetNullValue=Chọn album}"
                                               TextTrimming="CharacterEllipsis"/>
                                    <fa:IconBlock Grid.Column="1" Icon="ChevronDown" Foreground="#B0B0B0" FontSize="14"/>
                                </Grid>
                            </Button>

                            <Popup IsOpen="{Binding IsAlbumDropdownOpen, Mode=TwoWay}"
                                   PlacementTarget="{Binding ElementName=AlbumDropdownButton}"
                                   Placement="Bottom"
                                   VerticalOffset="6"
                                   StaysOpen="False"
                                   AllowsTransparency="True"
                                   PopupAnimation="Fade">
                                <Border Width="{Binding ActualWidth, ElementName=AlbumDropdownButton}"
                                        Background="{DynamicResource NavActiveTextBrush}"
                                        BorderBrush="#3A3533"
                                        BorderThickness="1"
                                        CornerRadius="10"
                                        Padding="6">
                                    <ScrollViewer MaxHeight="280" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                        <ItemsControl ItemsSource="{Binding MyAlbums}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Background="Transparent"
                                                            BorderThickness="0"
                                                            Padding="12,10"
                                                            HorizontalContentAlignment="Left"
                                                            Foreground="{DynamicResource AppTextBrush}"
                                                            Command="{Binding DataContext.SelectAlbumCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                            CommandParameter="{Binding}">
                                                        <Button.Style>
                                                            <Style TargetType="Button">
                                                                <Setter Property="Background" Value="Transparent"/>
                                                                <Style.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter Property="Background" Value="#3A3533"/>
                                                                    </Trigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Button.Style>
                                                        <TextBlock Text="{Binding name}"/>
                                                    </Button>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>

                        <Button Grid.Column="4"
                                Command="{Binding ChooseLyricFileCommand}"
                                 Background="{DynamicResource WindowBackgroundBrush}"
                                 Foreground="{DynamicResource AppTextBrush}"
                                BorderThickness="0"
                                Padding="14,10"
                                Content="{Binding SelectedLyricFileName, TargetNullValue=Chọn file Lyric}"/>
                    </Grid>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 18 0 0">
                        <Button Content="Hủy"
                                Margin="0 0 12 0"
                                Padding="26,10"
                                Background="#4B5563"
                                Foreground="White"
                                BorderThickness="0"
                                Command="{Binding CloseWindowCommand}"
                                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
                        <Button Content="Lưu"
                                Padding="26,10"
                                Background="{DynamicResource CurrentColor}"
                                Foreground="White"
                                BorderThickness="0"
                                Command="{Binding UploadSongConfirmCommand}"
                                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
                    </StackPanel>
                </StackPanel>
            </Grid>

            <TextBlock Grid.Row="3"
                       Margin="0 16 0 0"
                       FontSize="11"
                       Foreground="{DynamicResource AppTextBrush}"
                       TextWrapping="Wrap"
                       Text="Bằng cách tiếp tục, bạn đồng ý cho phép TÔI truy cập vào hình ảnh bạn đã chọn để tải lên. Vui lòng đảm bảo bạn có quyền tải lên hình ảnh."/>
        </Grid>
    </Border>
</Window>
