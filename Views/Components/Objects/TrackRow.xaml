<UserControl x:Class="Vibra_DesktopApp.Views.Components.TrackRow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:fa="clr-namespace:FontAwesome.Sharp;assembly=FontAwesome.Sharp"
             mc:Ignorable="d"
             d:DesignHeight="64" d:DesignWidth="900">

    <Border x:Name="Root"
            CornerRadius="10"
            Background="Transparent"
            Padding="10"
            Margin="0 0 0 6">
        <Border.Style>
            <Style TargetType="Border">
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#141414"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="48"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Leading area: index or play/pause on hover -->
            <Grid Width="48" Height="40" VerticalAlignment="Center">
                <TextBlock x:Name="IndexText"
                           Text="{Binding Index}"
                           Foreground="{DynamicResource AppTextMutedBrush}"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsMouseOver, ElementName=Root}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <fa:IconBlock x:Name="HoverIcon"
                              FontSize="18"
                              Foreground="{DynamicResource AppTextBrush}"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Visibility="Collapsed">
                    <fa:IconBlock.Style>
                        <Style TargetType="fa:IconBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsMouseOver, ElementName=Root}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </fa:IconBlock.Style>
                    <fa:IconBlock.Icon>
                        <MultiBinding Converter="{StaticResource TrackRowLeadingIconConverter}">
                            <Binding ElementName="Root" Path="IsMouseOver" />
                            <Binding Path="IsCurrentTrack" />
                            <Binding Path="IsPlaying" />
                        </MultiBinding>
                    </fa:IconBlock.Icon>
                </fa:IconBlock>
            </Grid>

            <!-- Track info -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="{Binding Track.name}"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource AppTextBrush}"
                           TextTrimming="CharacterEllipsis"/>
                <TextBlock Text="{Binding Track.author.name}"
                           FontSize="12"
                           Foreground="{DynamicResource AppTextMutedBrush}"
                           TextTrimming="CharacterEllipsis"/>
            </StackPanel>

            <!-- Actions -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Background="Transparent"
                        BorderThickness="0"
                        Padding="6"
                        Margin="0,0,6,0"
                        Command="{Binding AddToWaitlistCommand}">
                    <fa:IconBlock Icon="List" Foreground="{DynamicResource AppTextMutedBrush}" FontSize="16" />
                </Button>
            </StackPanel>

            <!-- Make row clickable (exclude actions column so buttons remain clickable) -->
            <Border Background="Transparent" Grid.ColumnSpan="2">
                <Border.InputBindings>
                    <MouseBinding Gesture="LeftClick"
                                 Command="{Binding PlayOrPauseCommand}"/>
                </Border.InputBindings>
            </Border>
        </Grid>
    </Border>

</UserControl>
